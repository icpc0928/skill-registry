# API 文件 — {專案名稱}

> 自動生成於 {日期}
> Base URL: `{BASE_URL}`

---

## 概覽

| 項目 | 說明 |
|------|------|
| 專案名稱 | {名稱} |
| 技術棧 | {框架 + 語言} |
| 認證方式 | Bearer Token (JWT) |
| 資料格式 | JSON |

---

## 認證

### 登入取得 Token

```bash
curl -s {BASE_URL}/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"password"}'
```

**Response:**
```json
{
  "token": "eyJhbGci..."
}
```

### 使用 Token

```
Authorization: Bearer {token}
```

---

## API 總覽

### Public（不需登入）

| Method | Path | 說明 |
|--------|------|------|
| | | |

### User（需要登入）

| Method | Path | 說明 |
|--------|------|------|
| | | |

### Admin（需要管理員）

| Method | Path | 說明 |
|--------|------|------|
| | | |

---

## API 詳細說明

（按模組分組，每個 endpoint 一個區塊）

### {模組名}

#### {API 說明}

`{METHOD} {PATH}`

**權限**：Public / User / Admin

**查詢參數**：

| 參數 | 型別 | 必填 | 預設值 | 說明 |
|------|------|------|--------|------|
| | | | | |

**Request Body**：

| 欄位 | 型別 | 必填 | 驗證規則 | 說明 |
|------|------|------|---------|------|
| | | | | |

**Response** `200`：

```json
{
}
```

**錯誤回應**：

| 狀態碼 | 說明 |
|--------|------|
| 400 | 參數錯誤 |
| 401 | 未認證 |
| 403 | 權限不足 |
| 404 | 資源不存在 |

**curl 範例**：

```bash
curl -s {BASE_URL}{PATH}
```

---

## 錯誤碼對照表

| 狀態碼 | 說明 | 常見原因 |
|--------|------|---------|
| 200 | 成功 | |
| 201 | 建立成功 | POST 新增資源 |
| 400 | 請求錯誤 | 參數格式錯誤、缺少必填欄位 |
| 401 | 未認證 | Token 缺失或過期 |
| 403 | 權限不足 | 用戶無此操作權限 |
| 404 | 找不到 | 資源不存在 |
| 409 | 衝突 | 資源已存在（如重複 email） |
| 500 | 伺服器錯誤 | 後端異常 |

---

## 資料模型

（列出主要的 Entity / Model 及其欄位）

### {Model 名稱}

| 欄位 | 型別 | 說明 |
|------|------|------|
| | | |
